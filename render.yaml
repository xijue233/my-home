# Render 一键部署配置
# 在 Render 新建 Blueprint（蓝print）并关联本仓库即可，无需手动填命令。
services:
  - type: web
    name: renovation-board
    runtime: node
    plan: free
    buildCommand: "npm install"
    startCommand: "node server.js"
    healthCheckPath: "/"
